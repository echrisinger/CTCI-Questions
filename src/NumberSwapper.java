
public class NumberSwapper {
	//assume you have two points a and b. compute the difference between the points: a-b=x
	//start aref: a     bref: b
	//mid   aref: x     bref: b   replaced a with difference
	//      aref: x     bref: x+b = a ---> aref: x bref: a
	//      aref: a - x = b bref: a
}
